let testName1: string = "Login Test";

function  add_ts(a: number, b: number): number {
    return a + b;
}

console.log(testName1); //prints the test name
console.log(add_ts(5, 10)); //prints the sum of 5 and 10
